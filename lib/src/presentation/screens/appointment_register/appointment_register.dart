import 'package:flutter/material.dart';

class AppointmentRegister extends StatelessWidget {
  const AppointmentRegister({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Add your appointment"),
      ),
    );
  }
}